import React from 'react';
import UserProfile from '../User/UserProfile'; // Asegúrate de que la ruta sea correcta

const Profile = () => {
  return (
    <div>
      <UserProfile />
    </div>
  );
};

export default Profile;
